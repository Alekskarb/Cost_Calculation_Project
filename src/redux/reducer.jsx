let initialState = {
        length: 1,
        width: 1,
        height: 2.5,
        floorage: 1
};

export const reducer = (state = initialState, action)=> {
    switch (action.type) {
        // case STEP1:
    }
    return state
};
