let initialState = {
    numberStep: 1,
    stepNumber: 'step#1',
    stepName: 'Add Your Room Data',
    isConditionsTrue: true,
};

export const reducer = (state = initialState, action)=> {
    switch (action.type) {
        // case STEP1:
    }
    return state
};
